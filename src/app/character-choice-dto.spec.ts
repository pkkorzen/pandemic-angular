import { CharacterChoiceDTO } from './character-choice-dto';

describe('CharacterChoiceDTO', () => {
  it('should create an instance', () => {
    expect(new CharacterChoiceDTO()).toBeTruthy();
  });
});
